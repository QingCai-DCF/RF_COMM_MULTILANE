# P10.1R rotating immutable offline build

- Status: `PASS`
- Test ID: `P10_1R-ROTATING-IMMUTABLE-OFFLINE-BUILD`
- Hardware actions executed: `false`
- Current-run hardware authorization: `false`

The `rotating` functional bitstream, XSA, BSP, ELF, and shutdown bitstream are content-addressed and source-bound.

This routed-build PASS has no hardware acceptance meaning.
