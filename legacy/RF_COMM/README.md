# Legacy RF_COMM Reference

Everything below `legacy/RF_COMM/` is historical reference only.

It is:

- not a canonical build input;
- not a replacement for `PROJECT_CONSTRAINTS.txt`;
- not current acceptance evidence;
- read-only source material from the superseded project.

The untracked historical ALINX training PDF found under `legacy/RF_COMM/docs/` is not automatically committed because redistribution permission is unverified. Its exact filename, size, SHA256, and local-only disposition are recorded in `legacy/RF_COMM/docs/LOCAL_ONLY_MANIFEST.json` and the repository organization evidence.
